services:
  - type: web
    name: saas-platform-backend
    runtime: python
    buildCommand: cd backend && pip install --upgrade pip && pip install poetry && poetry config virtualenvs.create false && poetry install --no-dev
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
