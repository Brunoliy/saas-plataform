input {
  beats {
    port => 5044
  }
}

filter {
  if [fields][service] == "saas-platform-backend" {
    grok {
      match => { "message" => "%{TIMESTAMP_ISO8601:timestamp} %{LOGLEVEL:level} %{GREEDYDATA:message}" }
    }
    date {
      match => [ "timestamp", "ISO8601" ]
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "saas-platform-logs-%{+YYYY.MM.dd}"
  }
} 